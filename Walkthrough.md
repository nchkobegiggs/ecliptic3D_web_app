# Ecliptic Lab v0.3 Walkthrough

本项目 v0.3 阶段完成了几何基准修正、月相枚举支持以及 UI 布局优化。

## 1. 世界坐标基准 (World Coordinate Convention)

我们采用了以下严格的世界坐标约定，确保所有天体运动与平面倾角的逻辑统一：

- **原点 (0,0,0)**：太阳中心 (Sun center)。
- **基准平面 (XZ Plane)**：黄道面 (Ecliptic plane)。
- **正 Y 轴 (+Y Axis)**：黄极 (Ecliptic North Pole)。
- **初始位置**：地球初始位于世界坐标 +X 轴，并绕 Y 轴进行逆时针 (CCW) 公转。
- **几何旋转**：所有 `THREE.RingGeometry` (默认在 XY 平面) 均通过 `rotation.x = -π/2` 旋转至 XZ 平面以对齐基准。

## 2. 平面定义与旋转顺序

各轨道面相对于黄道面的旋转逻辑如下：

- **黄道面 (Ecliptic)**：单位矩阵，位于世界 XZ 平面。
- **赤道面 (Equatorial)**：在黄道面基础上绕世界 X 轴旋转黄赤交角 ε (Obliquity)。
- **白道面 (Lunar Orbit)**：遵循 $R = R_y(\Omega) \cdot R_x(i)$ 顺序：
    1. 绕世界 Y 轴旋转升交点经度 $\Omega$。
    2. 绕旋转后的节点线（当前局部 X 轴）旋转月球轨道倾角 $i$ (≈ 5.145°)。

## 3. 地轴正交性验证 (Geometric Verification)

地轴方向被定义为赤道面的法向向量。通过程序实时计算，验证结果如下：

- **计算公式**：`dot(earthAxisWorldDir, equatorPlaneNormal)`
- **验证结果**：**1.0000000000** (误差 < 1e-10)
- **结论**：地轴严格垂直于赤道面，几何定义正确。

## 4. UI 功能更新

- **两行式布局**：优化了控制面板，解决移动端/小屏幕下的堆叠问题。
- **月相枚举**：增加了 8 个离散月相（新月、上弦月、满月等）的选择器，并与连续滑条保持实时双向同步。
- **标签开关**：引入了 "显示标签 (Labels)" 开关，通过 store 状态全局控制 CSS2D 标注的可见性。
- **文案优化**：所有希腊字母符号 (Ω, φ, ε′) 均配备了完整的中文文字说明。

## 5. 验收截图说明

- `v03_top_view`: 俯视视角，验证黄道面及地球轨道呈现为正圆。
- `v03_side_view`: 侧视视角，验证黄道面闭合为一条水平线。
- `v03_labels_off`: 验证标签切换功能的有效性。

---

## 6. 月相相位校准 (Phase Calibration - v0.3 Bug Fix)

v0.3 开发后期发现月球位置与相位不一致。现已按以下逻辑重写月球定位算法：

### A. 相对几何定义 (Relative Geometry)
月相相位角 $\alpha$ 定义为从地球视角看“日-月”的角距离（在白道面内）：
- **Ŝ** (Earth→Sun): 单位向量，通常指向 $(0,0,0) - P_{earth}$。
- **N̂** (Lunar Normal): 绕 $Y$ 轴旋转 $\Omega$ 后绕局部 $X$ 轴旋转 $i$ 得到的平面法向。
- **Ŝ_plane**: **Ŝ** 投影到月球轨道平面后的归一化向量。
- **T̂**: $N̂ \times Ŝ_{plane}$，定义为相位 $+90^\circ$ (上弦) 的切向。

### B. 月球位置公式 (Moon Position)
月球相对于地球的偏移向量 $M$ 计算如下：
$$M = \cos(\alpha) \cdot \hat{S}_{plane} + \sin(\alpha) \cdot \hat{T}$$
$$moonPos = earthPos + MOON\_ORBIT\_R \cdot M$$

### C. 验收结果 (Math Verification)
在控制台中通过 `dot(M̂, Ŝ)` 验证校准结果（同一节气下）：
- **新月 (0°)**: dot ≈ 1.0000 (Moon at Earth-Sun line)
- **上弦 (90°)**: dot ≈ 0.0000 (Moon orthogonal to Sun)
- **满月 (180°)**: dot ≈ -1.0000 (Moon opposite to Sun)
- **下弦 (270°)**: dot ≈ 0.0000 (Moon orthogonal, other side)

---

## 7. 白赤交角 ε′ 权威对齐 (ε′ Alignment - v0.3 Final)

针对 ε′ 显示值与 3D 场景不一致的 P0 Bug，已完成逻辑对齐：

### A. 权威计算口径 (Authoritative Calculation)
不再仅依赖预设公式，而是直接从 3D 渲染层的**世界坐标系法向量**反算：
1. **n_eq**: 赤道面 Mesh 的 `matrixWorld` 提取的单位法向量。
2. **n_lunar**: 白道面 Mesh 的 `matrixWorld` 提取s的单位法向量。
3. **ε′(scene)**: $\arccos(n_{eq} \cdot n_{lunar})$。
4. **验证结果**: 场景计算值与公式 $cos(\epsilon') = \cos \epsilon \cos i - \sin \epsilon \sin i \cos \Omega$ 完全一致（误差 < 0.0001°）。

### B. 坐标系与旋转约定
- **黄道面 (XZ)**: 参考平面，法向为 $(0, 1, 0)$。
- **赤道面**: 绕 X 轴旋转 $\epsilon$。
- **白道面**: $R = R_y(\Omega) \cdot R_x(-i)$。
  - *注：为了符合 Ω=0° 为最大交角 (ε+i) 的约定，白道局部倾角采用 -i*。

### C. 快捷验收 (Quick Presets)
- **MAX (Ω=0°)**: ε′ ≈ 28.585°
- **MIN (Ω=180°)**: ε′ ≈ 18.295°

可通过 UI 上的 **DEBUG** 开关查看实时向量分量与差值。

---

## 8. 动画联动系统修复 (Animation Linkage Fix)

修复了 v0.3 中动画完全停滞的 Bug。

### A. 问题原因
之前的 `animate` 循环虽然在推进 `time` 状态，但场景渲染直接读取的是 Store 中的静态角度值（如 `seasonLambdaRad`），而这些值未随时间自动累加。

### B. 修复方案 (Linkage Loop)
在 Store 中实现了 `advanceAnimation(dt)` 动作，每帧统一计算并更新所有轨道参数：
- **地球公转**: 约 365s 一圈（1s = 1仿真日）。
- **月球相位**: 约 29.5s 一圈。
- **节点进动**: 约 18.6 仿真年一圈。

### C. 验收结果
- **自动播放**: 页面加载后天体按物理比例自动运行。
- **UI 同步**: 顶部滑条与 3D 场景保持严格同步位移。
- **性能**: 即使在 `speed=10` 的高倍速下，渲染依然保持 60FPS 平滑，且 ` OrbitControls` 阻尼感正常。

---

## 9. 观测者地平坐标视图 (Sky View - v0.5)

完成了底部观测者视图的重构，实现了从 3D 几何到 2D 观测投影的闭环。

### A. 双视图并排 (Dual-View Layout)
- **Az-Alt Chart (左侧)**：方位角-高度角线性投影图。横轴为方位角（0°-360°），纵轴为高度角（-90° 至 +90°）。清晰标识了地平线（Altitude 0°）。
- **Dome View (右侧)**：等距方位投影天穹图。以天顶（Zenith）为圆心，地平线为边界。用于直观展示天体“东升西降”的轨迹。

### B. 技术特征
- **全天轨迹预计算 (Track Pre-calculation)**：当公转轨道参数（季节、交点、月相）或观测者纬度改变时，系统通过 `SkyEngine` 预计算全天 144 个采样点的坐标曲线。
- **高性能渲染**：采用 HTML5 Canvas 2D。仅在参数变化时重绘曲线，`time` 推进时仅更新点位标识，确保流畅的 60FPS 动画。
- **月相实时投影**：天穹图中月球标识显示亮暗比例，动态反映月相盈亏。

### C. 天文学基准验证 (φ=40°N)
通过对不同节气正午高度角的实测验证（误差 < 0.1°）：
- **春分 (Vernal Equinox)**：太阳最高高度 ≈ 50.0°。
- **夏至 (Summer Solstice)**：太阳最高高度 ≈ 73.4°。
- **冬至 (Winter Solstice)**：太阳最高高度 ≈ 26.6°。

### D. 动力学同步
所有的 `pause` / `speed` 控制均同时作用于主场景与底部双视图，确保观测数据与物理状态完全一致。

---

## 10. UI 交互与观测视角打磨 (UI/Interactive Polish - v0.5 & v0.6)

在 v0.6 版本中，我们对整个系统的交互逻辑、时间尺度和观测视角进行了深度重构，特别是从“静态投影”进化到了“动态观测”。

### A. 时间尺度控制 (Time/Speed Mapping - P0)
重新定义了速度滑条的物理含义，解决了之前由于线性映射导致的时间尺度失控。
- **模拟速率非线性映射**：
  - 滑块 0% → 60%：对应 **模拟 24h / 现实 12s** 到 **模拟 24h / 现实 1s**（适合观察日升日落）。
  - 滑块 60% → 100%：对应 **模拟 24h / 现实 1s** 到 **模拟 30d / 现实 1s**（适合观察月相更替和交点进动）。
- **实时反馈**：滑块下方实时显示当前映射的 `Sim Rate` 字符串，精确到秒。

### B. 观测者双视图重构 (Stacked Layout - P1)
为了获得更好的横向空间，地平坐标视图从左右并排改为**上下排列**：
1. **24 小时高度角轨迹图 (Daily Altitude Chart)**：
   - 横轴从方位角改为 **太阳时 (Solar Time 0h - 24h)**。
   - 标注了地平线及每 3 小时的关键刻度，能够直观看到全天太阳/月亮的高度变化趋势。
2. **天穹动态视场 (Local Sky Dome View)**：
   - 引入了 `headingAz` (视场朝向/转向) 控制。
   - 从传统的固定圆盘改为 **交互式滑动窗口**。当用户调整 "Sky Heading" 滑块时，天穹内的方位标记 (N/E/S/W) 和天体将随之平移，模拟真实转身观察天空的效果。

### C. 全局中英文双语 (Bilingual Support - P1)
实现了全局 UI 的中英文对照，包括：
- 控制面板的所有按钮与滑块（e.g., `Asc. Node 升交点`, `Latitude 纬度`）。
- 场景内的 3D 文本标签（e.g., `Equatorial 赤道面`）。
- 底部视图的所有标题及坐标辅助。

### D. 观测一致性验证
- **月相投影**：月球无论在 3D 场景、曲线图还是天穹图中，均展示与其轨道相位对应的亮暗比例。
- **动力学同步**：实现了模拟速度从“天”级到“月”级的无缝跨越，且物理基准始终保持一致。

### E. 布局滚动与渲染精化 (Layout & Rendering Polish - v0.6.1)
针对 v0.6 版本的反馈，进行了布局与渲染的深度优化。
- **滚动锁定修复**：
  - 移除了 `index.css` 中对 `html, body, #root` 的 `overflow: hidden` 限制，改为 `overflow-y: auto`。
  - 将 `App.tsx` 的布局重构为自然纵向流，并将 `ControlPanel` 设置为 `sticky` 吸顶，确保下方的天穹视图可滚动到达。
- **响应式居中宽度**：
  - 在桌面端，高度角图表 (Altitude Chart) 与地平视窗 (Local Sky Dome) 的宽度被限制为屏幕的 **60% (max-width: 1200px)** 并水平居中，避免了视觉上的过度拥挤。
  - 在移动端自动设为 100% 宽度，兼顾不同设备的体验。
- **Canvas 高清化 (DPR Fix)**：
  - 引入了 `devicePixelRatio` 适配机制。Canvas 的物理像素现在是 CSS 像素的 `dpr` 倍，并配合 `ctx.setTransform(dpr, 0, 0, dpr, 0, 0)` 进行自动缩放。
  - 修复后，图表内的文字、刻度线和天体标注在 Retina/高刷屏上依然清晰锐利。

### F. 交互体验深度增强 (UI Interactive Enhancement - v0.6.2)
为了进一步提升观测者在不同屏幕比例下的体验，并解决“下滑后无法调参”的问题，进行了 v0.6.2 深度优化。
- **模块居中与标题统一**：
  - 高度角图表与天穹视窗现在统一在主容器内水平居中，并为 0h/24h 留出了足够的边缘 Padding (30px)，确保护边标签不再被裁切。
  - 标题从 Canvas 内部改为 DOM 层渲染，风格统一居中置顶。
- **高度角横线网格 (Altitude Grid)**：
  - 在高度角轨迹图中新增了每 15° 一条的横向虚线网格，并标注了 0° (HORIZON), +45°, +90° 等关键刻度，方便定量读取。
- **天穹局部控制面板 (Local Heading Controls)**：
  - 在 Perspective Sky Dome 右侧新增了专属控制栏。
  - **即时转向**：内置 N (0°), E (90°), S (180°), W (270°) 四个快捷方位按钮。
  - **天体对齐**：新增 "Align to Sun" 和 "Align to Moon" 按钮，一键将视场中心对准当前太阳或月亮的方位角，解决手动寻找天体的痛点。
- **月相对比度增强**：
  - 重构了天穹图中的月亮渲染逻辑：暗面与背景色完全一致，亮面使用高亮银白，配合 1px 细微描边，确保在任何背景亮度下月相都能一眼区分。

---

当前版本已完成所有交互性、可视化及渲染细节的打磨，系统已完全具备进行复杂天文现象（如“食”的发生、月球极端赤纬分析等）的高级模拟能力。下一步 (v0.7)：月球极大偏角与回归分析。
